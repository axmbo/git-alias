---
name: Bug
about: Algo no git-alias não funciona como documentado
title: ''
labels: bug
assignees: ''
---

## Versões (obrigatório)

<!-- Sem isso não dá para saber se o bug já foi corrigido numa versão mais
     nova, nem reproduzir num ambiente equivalente. -->

```
$ git alias --version
$ git --version
```

- **Sistema operacional e versão:**
  <!-- ex.: Ubuntu 24.04, macOS 15, ... -->
- **Shell usado para rodar `git alias`/`install.sh`** (se relevante):
  <!-- ex.: bash 5.2, zsh 5.9, dash ... -->

## O que aconteceu

<!-- Comando exato rodado e a saída (stdout/stderr) completa. -->

```
$ git alias ...
```

## O que eu esperava

## Como reproduzir

<!-- Passo a passo, a partir de um HOME limpo se possível (o mesmo padrão
     que tests/git-alias.sh usa: HOME/GIT_CONFIG_GLOBAL temporários). -->

## `git alias --doctor` (se o bug envolve instalação/detecção do arquivo de aliases)

```
$ git alias --doctor
```
